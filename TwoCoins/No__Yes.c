#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(int d, char** e){
	int a, b, c;
	char *f, *ff = (char*)1, *fn;

	for(strtok(0[e], "\\"); ff; fn = ff, ff = strtok(NULL, "\\"));
	f = strtok(fn, ".");
	(d << d)[f] = '\n';
	((d << d << d) - d)[f] = '\0';

	scanf("%d %d %d", &a, &b, &c);
	printf((((unsigned int)(c - a - b - d) >> 15 & 0x0001) << 2) + f);

}
